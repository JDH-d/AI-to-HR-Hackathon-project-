[project]
name = "ai-hr-assistant"
version = "0.1.0"
description = "MVP AI HR Assistant with OpenAI + RAG for Russian-speaking teams"
requires-python = ">=3.11"

[tool.ruff]
line-length = 100
target-version = "py311"

